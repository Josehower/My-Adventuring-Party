exports.up = async (sql) => {
  await sql`
	CREATE TABLE IF NOT EXISTS expedition (
		expedition_id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
		game_id integer NOT NULL REFERENCES game_instance (game_id) ON DELETE CASCADE ON UPDATE CASCADE,
		helden_id integer UNIQUE NOT NULL REFERENCES helden (helden_id) ON DELETE CASCADE ON UPDATE CASCADE,
		expedition_start_date TIMESTAMP NOT NULL DEFAULT NOW()
	);`;
};

exports.down = async (sql) => {
  await sql`
	DROP TABLE IF EXISTS expedition;`;
};
