exports.up = async (sql) => {
  await sql`
CREATE TABLE IF NOT EXISTS action_speed (
	action_speed_id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
	speed_name varchar(10) NOT NULL
);`;

  await sql`
CREATE TABLE IF NOT EXISTS target_keyword (
	target_id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
	target_keyword_name varchar(10) NOT NULL
);`;

  await sql`
CREATE TABLE IF NOT EXISTS action_type (
	action_type_id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
	action_type_name varchar(20),
	action_type_desc text,
	action_speed_id integer REFERENCES action_speed (action_speed_id) ON DELETE CASCADE
);`;
  await sql`
CREATE TABLE IF NOT EXISTS combat_action (
	combat_action_id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
	action_name varchar(50),
	action_desc text,
	effect_ref integer,
	action_type_id integer REFERENCES action_type (action_type_id) ON DELETE CASCADE,
	target_id integer REFERENCES target_keyword (target_id) ON DELETE CASCADE
);`;
};

exports.down = async (sql) => {
  await sql`
DROP TABLE IF EXISTS action_speed, target_keyword, action_type, combat_action;`;
};
