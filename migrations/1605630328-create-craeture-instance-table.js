exports.up = async (sql) => {
  await sql`
CREATE TABLE IF NOT EXISTS creature_instance (
	creature_instance_id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
	creature_id integer REFERENCES enemy_creatures (creature_id) ON DELETE CASCADE,
	combat_id integer REFERENCES combat_instance (combat_id) ON DELETE CASCADE,
	sa_avaliable integer NOT NULL DEFAULT 2,
	instance_ve integer NOT NULL,
	slot_position integer NOT NULL
);`;
};

exports.down = async (sql) => {
  await sql`
DROP TABLE IF EXISTS creature_instance;`;
};
