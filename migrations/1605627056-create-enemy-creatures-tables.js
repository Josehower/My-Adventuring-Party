exports.up = async (sql) => {
  await sql`
CREATE TABLE IF NOT EXISTS creature_stats_set (
	stats_id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
	ve_vital_energy integer NOT NULL,
	ap_action_power integer NOT NULL,
	sd_supernatural_defense integer NOT NULL,
	pd_physical_defense integer NOT NULL,
	lvl_level integer,
	sa_special_actions integer
);`;
  await sql`
CREATE TABLE IF NOT EXISTS enemy_creatures (
	creature_id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
	name varchar(50) NOT NULL,
	creature_type_id integer NOT NULL REFERENCES creature_type (creature_type_id) ON DELETE CASCADE,
	stats_id integer NOT NULL REFERENCES creature_stats_set (stats_id ) ON DELETE CASCADE
);`;
};

exports.down = async (sql) => {
  await sql`
DROP TABLE IF EXISTS enemy_creatures;`;
  await sql`
DROP TABLE IF EXISTS creature_stats_set;`;
};
