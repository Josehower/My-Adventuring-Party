exports.up = async (sql) => {
  await sql`
CREATE TABLE IF NOT EXISTS helden_instance (
	helden_instance_id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
	helden_id integer REFERENCES helden (helden_id) ON DELETE CASCADE,
	combat_id integer REFERENCES combat_instance (combat_id) ON DELETE CASCADE,
	sa_avaliable integer NOT NULL DEFAULT 2,
	instance_ve integer NOT NULL
);`;
};

exports.down = async (sql) => {
  await sql`
DROP TABLE IF EXISTS helden_instance;`;
};
