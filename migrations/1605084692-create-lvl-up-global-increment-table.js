exports.up = async (sql) => {
  await sql`
	CREATE TABLE IF NOT EXISTS stats_increment_by_lvl_up (
		stats_increment_id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
		new_lvl integer NOT NULL,
		new_total_sa integer NOT NULL,
		ve_ap_increment integer NOT NULL,
		def_increment integer NOT NULL,
		ve_extra_increment integer NOT NULL,
		turns_into_stone boolean DEFAULT false
	);`;
};

exports.down = async (sql) => {
  await sql`
	DROP TABLE IF EXISTS stats_increment_by_lvl_up;`;
};
